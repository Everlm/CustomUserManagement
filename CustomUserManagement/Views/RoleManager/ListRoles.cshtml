@model IEnumerable<IdentityRole>

@{
    ViewBag.Title = "All Roles";
}

<br />
<h1>Roles</h1>
<br />

<p>
    <a class="btn btn-primary" asp-action="CreateRole" asp-controller="RoleManager">Add</a>
</p>
<table class="table">
    <thead>
        <tr class="">
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>

                <td>
                    <a class="btn btn-outline-warning" asp-controller="RoleManager" asp-action="EditRole" asp-route-id="@item.Id">Edit</a>

                    <form asp-action="DeleteRole" asp-route-id="@item.Id" method="post">
                        <span id="confirmDeleteSpan_@item.Id" style="display:none">
                            <span>Are you sure you want to delete?</span>
                            <button type="submit" class="btn btn-danger">Yes</button>
                            <a href="#" class="btn btn-primary"
                           onclick="confirmDelete('@item.Id', false)">No</a>
                        </span>

                        <span id="deleteSpan_@item.Id">
                            <a href="#" class="btn btn-danger"
                           onclick="confirmDelete('@item.Id', true)">Delete</a>
                        </span>
                    </form>
                    <a class="btn btn-outline-info" asp-controller="RoleManager" asp-action="DeleteRole" asp-route-id="@item.Id">Details </a>
                </td>
            </tr>
        }
    </tbody>
</table>

@section Scripts {
    <script src="~/js/site.js"></script>
}